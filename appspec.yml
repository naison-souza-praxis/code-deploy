version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/web
permissions:
  - object: /home/ubuntu/web
    pattern: "**"
    owner: www-data
    group: www-data
    mode: 755
    type:
       - file
